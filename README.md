# Canvas Gradebook Dashboard — Refined UX

A sleeker Streamlit app with tabs, persistent filters, category controls, weight scenarios, and modern visuals.

## Run
```bash
pip install -r requirements.txt
streamlit run app.py
```